<?xml version="1.0" encoding="UTF-8"?>
<svg xmlns="http://www.w3.org/2000/svg" width="64" height="64" viewBox="0 0 64 64" fill="none">
  <!-- Circles nav icon (matches minimalist line-icons; color via currentColor) -->
  <g stroke="currentColor" stroke-width="3.5" stroke-linecap="round" stroke-linejoin="round" fill="none">
    <circle cx="26" cy="34" r="13"/>
    <circle cx="38" cy="34" r="13"/>
  </g>

  <!-- Small status dot (uses currentColor so it becomes green when active) -->
  <circle cx="49" cy="16" r="4.2" fill="currentColor"/>
</svg>
